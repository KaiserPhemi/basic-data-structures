// Singly Linked List: Similar to an array but it consist of a bunch of elements with no indices
// i.e. it consist of nodes that has value and a pointer to another node or null

/**
 * @desc
 */
class Node {
  constructor(nodeVal) {
    this.nodeVal = nodeVal;
    this.nextNode = null;
  }
}

/**
 * @desc
 */
class SinglyLinkedList {
  constructor() {}
}
